{
  "name": "@keyfall/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Keyfall game layer: arena, enemies, rendering, run lifecycle",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@keyfall/typing-core": "*"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "vite": "^5.4.0"
  }
}
